import type { GeneratorMetadata } from '@generators/generators'
import Xid from '@generators/Xid.vue'

export const xid: GeneratorMetadata = {
  identifier: 'xid',
  displayName: 'XID',
  component: Xid,
}
