import UuidMax from '@generators/UuidMax.vue'
import type { GeneratorMetadata } from '@generators/generators'

export const uuidMax: GeneratorMetadata = {
  identifier: 'uuid-max',
  displayName: 'UUID Max',
  component: UuidMax,
}
