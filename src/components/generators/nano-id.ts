import NanoId from '@generators/NanoId.vue'
import type { GeneratorMetadata } from '@generators/generators'

export const nanoId: GeneratorMetadata = {
  identifier: 'nano-id',
  displayName: 'Nano ID',
  component: NanoId,
}
