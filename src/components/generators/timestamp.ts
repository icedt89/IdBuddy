import Timestamp from '@generators/Timestamp.vue'
import type { GeneratorMetadata } from '@generators/generators'

export const timestamp: GeneratorMetadata = {
  identifier: 'timestamp',
  displayName: 'Timestamp',
  component: Timestamp,
}
